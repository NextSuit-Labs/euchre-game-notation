{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Euchre Game Notation (EGN)",
  "description": "JSON Schema for validating Euchre Game Notation (.egn) files.",
  "type": "object",
  "properties": {
    "fileType": {
      "type": "string",
      "const": "Euchre Game Notation"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "metadata": {
      "type": "object",
      "properties": {
        "matchId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "players": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 4,
          "maxItems": 4,
          "description": "Array of 4 player names, starting with Player 0 and proceeding clockwise."
        },
        "initialScore": {
          "type": "array",
          "items": { "type": "integer" },
          "minItems": 2,
          "maxItems": 2,
          "description": "Initial team scores. [Team 0/2 Score, Team 1/3 Score]."
        },
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "Date and time when the game was played."
        },
        "ruleset": {
            "type": "object",
            "properties": {
                "std": { "type": "boolean", "default": true },
                "canadian": { "type": "boolean", "default": false },
                "loner_lead": { "type": "string", "enum": ["LEFT_OF_DEALER", "LEFT_OF_LONER"], "default": "LEFT_OF_DEALER" },
                "farmer": { "type": "boolean", "default": false },
                "partners_best": { "type": "boolean", "default": false }
            }
        }
      },
      "required": ["players", "initialScore"]
    },
    "deals": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "dealNumber": { "type": "integer", "minimum": 0 },
          "initialState": {
            "type": "object",
            "properties": {
              "dealer": {
                "type": "integer",
                "minimum": 0,
                "maximum": 3
              },
              "upCard": { "$ref": "#/definitions/card" },
              "kitty": {
                "type": "array",
                "minItems": 0,
                "maxItems": 3,
                "items": { "$ref": "#/definitions/card" }
              }
            },
            "required": ["dealer", "upCard"]
          },
          "phases": {
            "type": "array",
            "minItems": 2,
            "maxItems": 2,
            "items": {
              "oneOf": [
                { "$ref": "#/$defs/biddingPhase" },
                { "$ref": "#/$defs/trickPlayPhase" }
              ]
            }
          }
        },
        "required": ["dealNumber", "initialState", "phases"]
      }
    }
  },
  "required": ["fileType", "version", "deals"],
  "definitions": {
    "card": {
      "type": "string",
      "pattern": "^([N9TJQKA])[shcd]$"
    },
    "biddingPhase": {
      "type": "object",
      "required": ["phaseNumber", "type", "calls", "isAlone"],
      "properties": {
        "phaseNumber": { "type": "integer" },
        "type": { "const": "EUCHRE_BIDDING" },
        "calls": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": { "type": "string", "enum": ["Pass", "Order", "s", "h", "d", "c"] }
        },
        "isAlone": { "type": "boolean" },
        "discard": { "$ref": "#/$defs/card" }
      }
    },
    "trickPlayPhase": {
      "type": "object",
      "required": ["phaseNumber", "type", "tricks"],
      "properties": {
        "phaseNumber": { "type": "integer" },
        "type": { "const": "TRICK_PLAY" },
        "tricks": {
          "type": "array",
          "minItems": 1,
          "maxItems": 5,
          "items": {
            "type": "array",
            "minItems": 3,
            "maxItems": 4,
            "items": { "$ref": "#/$defs/card" }
          }
        }
      }
    }
  }
}